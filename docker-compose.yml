version: '3.4'

services:
  daps:
    build:
      context: .
      network: host
    container_name: daps
    hostname: daps
    network_mode: host
    tty: true
    stdin_open: true
    volumes:
      - type: bind
        source: .
        target: /home/user
    entrypoint: daps
